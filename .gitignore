# Local secrets — NEVER commit your API key
config.local.json

# Remembered BallonsTranslator location (per-machine; auto-detected)
.bt_dir

# Python
__pycache__/
*.pyc
*.pyo

# Scratch / outputs (these never belong in the repo)
work/
*_translated/

# OS
.DS_Store
Thumbs.db
